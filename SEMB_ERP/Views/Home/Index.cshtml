@model SEMB_ERP.Models.LoginModel
@using Microsoft.AspNetCore.Mvc.Rendering;
@using Microsoft.AspNetCore.Http
@using System.Security.Claims;


@{
    //ViewData["Title"] = "Login Page";
    // string name = User.FindFirst("name")?.Value;
    // int changeLevel = ViewBag.changeLevel;
    string sesa_id = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
    string name = User.FindFirst("semb_erp_name")?.Value;
    string level = User.FindFirst("semb_erp_level")?.Value;
    string role = User.FindFirst("semb_erp_role")?.Value;
    int is_registered = 1;
    if (level == null)
    {
        is_registered = 0;
    }
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SEMB Material Non Stock</title>
    <!-- AdminLTE V3 -->
    <link href="~/Content/adminlte.min.css" rel="stylesheet" />
    <link href="~/lib/adminlte/plugins/fontawesome-free/css/all.css" rel="stylesheet" />
    <link href="~/lib/adminlte/plugins/fontawesome-free/css/fontawesome.css" rel="stylesheet" />
    <link href="~/lib/adminlte/plugins/fontawesome-free/css/solid.css" rel="stylesheet" />
    <link href="~/lib/bootstrap/dist/css/bootstrap.css" rel="stylesheet" />
    <link href="~/lib/adminlte/dist/css/adminlte.min.css" rel="stylesheet" />
</head>

<body class="bg">
    <div>
        <div class="container fluid mask mb-3 pb-5">
            <div class="row mt-5">
                <div class="bg-success" style="height:30px; opacity:0.5;">
                </div>
                <div class="bg-success" style="height:30px;">
                </div>

            </div>
            <div class="row align-items-center justify-content-center p-2">

                <div class="col-md-6 mt-5">
                    <div class="">
                        <div class="text-success">
                            <h1 style="font-size:50pt;"><i class="fas fa-chart-area"></i></h1>
                        </div>
                        <h1 class="text-success"><b>SEMB Material Non Stock</b></h1>
                        <h5>Digital Transformation</h5>

                        <div class="mt-5">
                            <p>version 1.0</p>
                        </div>

                    </div>
                </div>

                <div class="col-md-4 ">
                    <div class="align-items-center">
                        <h4>Welcome,</h4>
                        <h4>@name - @sesa_id</h4>
                        @if (is_registered == 1)
                        {
                            @* <div class="row">
                                <select class="form-control" name="level">
                                    <option value="Requestor|Requestor">Requestor</option>
                                    <option value="Finance|Finance">Finance</option>
                                </select>
                            </div> *@
                            <div class="row">
                                <div class="col-12 mt-3">
                                    <a type="submit" name="submit" class="btn btn-success btn-block btn-flat rounded" href="@Url.Action("Open")" @(is_registered == 0 ? "disabled" : "")><i class="fas fa-sign-in-alt"></i> SSO Login</a>
                                </div>
                            </div>
                        }
                        else
                        {
                            @* <div class="row">
                                <div class="col-12 mt-3">
                                    <a type="submit" name="submit" class="btn btn-success btn-block btn-flat rounded" @(is_registered == 0 ? "disabled" : "")><i class="fas fa-sign-in-alt"></i> Login</a>
                                </div>
                            </div> *@
                            <div class="row text-center">
                                <div class="col-12 mt-3 ">
                                    <div class="alert alert-danger">You don't have access,<br />Please contact admin for support.</div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>

        </div>

    </div>



    <footer class="container fluid p-2">
        <div class="row">

            <div class="d-flex align-items-center">
                <div class="ml-5">
                    <div class="text-white mt-2" style="font-size:10pt; ">
                        <ul style="list-style-type:none;">
                            <li>&copy;2025 - Digital Transformation</li>
                            <li>Schneider Electric Manufacturing Batam</li>
                        </ul>
                    </div>
                </div>
                <div class="ml-auto">
                    <img src="~/images/logo/se-lio.svg" class="d-inline-block align-text-top" style="max-width:80%;" />
                </div>
            </div>

        </div>
    </footer>



    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.js"></script>

    <style>

        .bg-mask {
            background-color: #000000;
        }

        .bg {
            background: url('@Url.Content("~/images/background/bg1.jpg")');
            width: 100%;
            background-size: cover;
            /*opacity: 0.5;*/
        }

        .mask {
            background: rgba(255, 255, 255, 0.83);
            border-radius: 2px;
        }

    </style>
</body>
</html>
